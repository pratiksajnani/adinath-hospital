# Adinath Hospital - Redirects (for Netlify compatibility)
# AWS Amplify uses rewrites/redirects in amplify.yml or console

# Clean URLs without .html
/login      /login.html     200
/portal     /portal/index.html     200
/forms      /forms/index.html     200
/store      /store.html     200
/book       /book.html      200

# 404 fallback (but not for actual files)
/*    /index.html    200
