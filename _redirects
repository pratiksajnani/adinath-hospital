# Adinath Hospital - AWS Amplify Redirects
# Note: Amplify ignores _redirects, use amplify.yml or console for redirects

# Keep this file for documentation purposes only
# All redirects should be configured in AWS Amplify Console
# or in amplify.yml customHeaders section

# Clean URLs (for reference):
# /login      -> /login.html
# /book       -> /book.html
# /store      -> /store.html
# /portal     -> /portal/index.html
